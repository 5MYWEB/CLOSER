<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.closer.model.mapper.CommentMapper">

    <insert id="addComment" parameterType="CommentDto">
        insert into comment(kind_pk, board_pk, userId, reply) values (#{kind_pk}, #{board_pk}, #{userId}, #{reply})
    </insert>

    <delete id="deleteComment" parameterType="CommentDto">
        delete from comment
        where comment_pk=#{comment_pk}
    </delete>

    <select id="isComment" parameterType="CommentDto" resultType="int">
        select count(*)
        from comment
        where comment_pk=#{comment_pk} and userId=#{userId}
    </select>

    <select id="countComment" parameterType="CommentDto" resultType="int">
        select count(*)
        from comment
        where kind_pk=#{kind_pk} and board_pk=#{board_pk}
    </select>

    <delete id="deleteAllComment" parameterType="CommentDto">
        delete from comment
        where kind_pk=#{kind_pk} and board_pk=#{board_pk}
    </delete>

</mapper>