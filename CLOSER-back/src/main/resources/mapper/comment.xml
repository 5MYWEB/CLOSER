<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.closer.model.mapper.CommentMapper">

<!--    <select id="commentList" parameterType="CommentDto" resultType="CommentDto">-->
<!--        select *-->
<!--        from comments-->
<!--        where kind_pk=#{kind_pk} and board_pk=#{board_pk}-->
<!--    </select>-->

<!--    <insert id="createComment" parameterType="CommentDto">-->
<!--        insert into comments(kind_pk, board_pk, userId, reply) values (#{kind_pk}, #{board_pk}, #{userId}, #{reply})-->
<!--    </insert>-->

<!--    <delete id="deleteComment" parameterType="int">-->
<!--        delete from comments-->
<!--        where comment_pk=#{comment_pk}-->
<!--    </delete>-->

<!--    <select id="isComment" parameterType="CommentDto" resultType="int">-->
<!--        select count(*)-->
<!--        from comments-->
<!--        where comment_pk=#{comment_pk} and userId=#{userId}-->
<!--    </select>-->

<!--    <select id="countComment" parameterType="CommentDto" resultType="int">-->
<!--        select count(*)-->
<!--        from comments-->
<!--        where kind_pk=#{kind_pk} and board_pk=#{board_pk}-->
<!--    </select>-->

<!--    <select id="findCommentUser" parameterType="int" resultType="String">-->
<!--        select userId-->
<!--        from comments-->
<!--        where comment_pk=#{comment_pk}-->
<!--    </select>-->

</mapper>