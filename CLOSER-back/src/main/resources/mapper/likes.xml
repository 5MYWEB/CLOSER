<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.closer.model.mapper.LikeMapper">

    <insert id="addLike" parameterType="LikeDto">
        insert into likes(kind_pk, board_pk, userId) values (#{kind_pk}, #{board_pk}, #{userId})
    </insert>

    <delete id="cancelLike" parameterType="LikeDto">
        delete from likes
        where kind_pk=#{kind_pk} and board_pk=#{board_pk} and userId=#{userId}
    </delete>

    <select id="isLike" parameterType="LikeDto" resultType="int">
        select count(*)
        from likes
        where kind_pk=#{kind_pk} and board_pk=#{board_pk} and userId=#{userId}
    </select>

    <select id="countLike" parameterType="LikeDto" resultType="int">
        select count(*)
        from likes
        where kind_pk=#{kind_pk} and board_pk=#{board_pk}
    </select>

    <delete id="deleteLike" parameterType="LikeDto">
        delete from likes
        where kind_pk=#{kind_pk} and board_pk=#{board_pk}
    </delete>

</mapper>